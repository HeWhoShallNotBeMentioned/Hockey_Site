<p id="notice"><%= notice %></p>

<h2>
  <%= @post.title %>
</h2>
<p><%= @post.body %></p>

Tags:
<% @post.tags.each do |tag| %>
  <%= tag.name  %> <%= link_to 'Edit', edit_post_tag_path(@post, tag) %> <%= link_to 'Delete', post_tag_path(@post, tag), data: {confirm: "Are you sure?", method: "delete"}%></li>
<% end %>
<br>
<%= link_to 'New Tag', new_post_tag_path(@post) %> |
<%= link_to 'Edit', edit_post_path(@post) %>
<br>
<%= link_to 'Back', posts_path %>

<h3>Comments</h3>
<% @post.comments.each do |comment| %>
  <%= comment.body  %> <%= link_to 'Edit', edit_post_comment_path(@post, comment) %> <%= link_to 'Delete', post_comment_path(@post, comment), data: {confirm: "Are you sure?", method: "delete"}%></li>
<br>
<% end %>
<br>



<%= link_to 'New Comment', new_post_comment_path(@post) %>
